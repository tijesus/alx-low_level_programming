#include <stdio.h>
/**
 * main - function to print program name
 * @argc: paramenter for counter
 * @argv: parameter for vector
 * Return: 0 success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
