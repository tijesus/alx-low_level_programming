#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphaStart, alphaEnd, alpha;

	alphaStart = 'a';
	alphaEnd = 'z';

	do {
		putchar(alpha);
	} while (alphaStart <= alphaEnd);
		continue;
	alphaStart = 'A'
	alphaEnd = 'Z'
	do {
		putchar(alpha);
	} while (alphaStart <= alphaEnd);

	putchar('\n');
	return (0);
}
